# 11.1 实验管理与追踪

## 1 技术简介

**技术定义**：实验管理（Experiment Tracking）是记录和管理机器学习/AI实验的系统性方法，包括超参数配置、训练指标、模型版本、代码状态等信息，使实验可复现、可比较、可追溯。实验管理是AI工程化的基础能力，对于团队协作和模型迭代至关重要。

实验管理的核心价值：
- **可复现性**：记录完整的实验配置，确保可以复现结果
- **可比较性**：对比不同实验的结果，选择最佳方案
- **可追溯性**：追踪实验来源，理解模型演进历史
- **团队协作**：共享实验结果，避免重复工作

**主流实验管理工具对比**：
| 工具 | 类型 | 特点 | 适用场景 |
|------|------|------|---------|
| **MLflow** | 开源 | 功能全面，社区活跃 | 通用场景 |
| **Weights & Biases** | 商业 | 可视化优秀 | 深度学习研究 |
| **TensorBoard** | 免费 | TensorFlow生态 | TensorFlow用户 |
| **Comet** | 商业 | 企业级功能 | 企业团队 |

## 2 技术背景

**为什么需要实验管理**：

1. **AI研发的特殊性**：
   - 实验数量庞大，超参数组合爆炸
   - 训练时间长，结果需要记录
   - 随机性导致结果需要多次验证

2. **团队协作需求**：
   - 多研究人员并行实验
   - 知识共享和经验传承
   - 避免重复实验

3. **生产环境要求**：
   - 模型审计和合规
   - 问题追溯和诊断
   - 持续改进的基础

**实验管理的核心要素**：
- 参数（Parameters）：模型配置、训练设置
- 指标（Metrics）：评估指标、训练曲线
- 产物（Artifacts）：模型文件、可视化图表
- 代码（Code）：代码版本、环境配置

## 3 核心概念

**Experiment（实验）**：
一个实验是一次模型训练或评估的完整记录，包含所有配置、结果和产物。

**Run（运行）**：
实验内的一次具体执行，记录单次训练的所有信息。

**Parameters（参数）**：
模型的超参数和配置，如学习率、batch size、网络结构等。

**Metrics（指标）**：
训练和评估过程中记录的数值，如准确率、损失值、延迟等。

**Artifacts（产物）**：
实验生成的任何文件，如模型权重、可视化图表、日志文件等。

**Tracking Server**：
用于存储和共享实验数据的服务器，支持团队协作。

## 4 工作原理

**实验追踪系统架构**：
```
实验代码 → Tracking API → Tracking Server → Web UI
                                    ↓
                              数据库存储
```

**MLflow的工作原理**：
1. 通过mlflow.start_run()开始一次实验
2. 使用log_params()记录参数
3. 使用log_metrics()记录指标
4. 使用log_artifact()保存文件
5. 通过mlflow ui查看结果

**数据存储模型**：
- Experiment：实验集合
- Run：单次实验运行
- Metric：时序指标数据
- Parameter：参数配置
- Artifact：文件产物

## 5 技术实现

**MLflow基本使用流程**：
1. 安装MLflow：`pip install mlflow`
2. 设置跟踪URI
3. 创建或获取实验
4. 记录参数、指标、产物
5. 通过UI查看结果

**实验设计的最佳实践**：
1. 使用有意义的实验名称
2. 记录所有可能影响结果的因素
3. 定期保存checkpoint
4. 使用版本控制管理代码

## 6 Python代码实现与逐行解析

```python
# MLflow实验管理示例（需要安装: pip install mlflow）
"""
MLflow是一个开源的机器学习生命周期管理平台，核心功能：
1. Tracking: 记录参数、指标、模型
2. Projects: 打包代码以可复现的方式运行
3. Models: 管理模型版本和部署
4. Registry: 模型版本注册中心
"""

def mlflow_tracking_example():
    """MLflow实验追踪示例"""
    try:
        import mlflow
        import mlflow.sklearn
        from sklearn.ensemble import RandomForestClassifier
        from sklearn.datasets import load_iris
        from sklearn.model_selection import train_test_split
        from sklearn.metrics import accuracy_score, precision_score, recall_score
        import numpy as np
        
        # 设置MLflow跟踪URI（可以是本地或远程服务器）
        mlflow.set_tracking_uri("./mlruns")  # 本地存储
        
        # 创建或获取实验
        experiment_name = "iris_classification"
        mlflow.set_experiment(experiment_name)
        
        # 加载数据
        iris = load_iris()
        X_train, X_test, y_train, y_test = train_test_split(
            iris.data, iris.target, test_size=0.2, random_state=42
        )
        
        # 定义要尝试的超参数组合
        param_grid = [
            {"n_estimators": 50, "max_depth": 3, "min_samples_split": 2},
            {"n_estimators": 100, "max_depth": 5, "min_samples_split": 2},
            {"n_estimators": 100, "max_depth": 10, "min_samples_split": 5},
        ]
        
        for i, params in enumerate(param_grid):
            # 开始一个运行（run）
            with mlflow.start_run(run_name=f"run_{i+1}"):
                print(f"\n运行 {i+1}: {params}")
                
                # 1. 记录参数
                mlflow.log_params(params)
                mlflow.log_param("dataset", "iris")
                mlflow.log_param("test_size", 0.2)
                
                # 2. 训练模型
                model = RandomForestClassifier(**params, random_state=42)
                model.fit(X_train, y_train)
                
                # 3. 评估
                y_pred = model.predict(X_test)
                accuracy = accuracy_score(y_test, y_pred)
                precision = precision_score(y_test, y_pred, average='weighted')
                recall = recall_score(y_test, y_pred, average='weighted')
                
                # 4. 记录指标
                mlflow.log_metric("accuracy", accuracy)
                mlflow.log_metric("precision", precision)
                mlflow.log_metric("recall", recall)
                
                # 5. 记录特征重要性
                for idx, importance in enumerate(model.feature_importances_):
                    mlflow.log_metric(f"feature_{idx}_importance", importance)
                
                # 6. 保存模型
                mlflow.sklearn.log_model(model, "model")
                
                # 7. 保存其他文件（如可视化）
                import matplotlib.pyplot as plt
                plt.figure(figsize=(10, 6))
                plt.bar(range(len(model.feature_importances_)), model.feature_importances_)
                plt.xlabel("Feature Index")
                plt.ylabel("Importance")
                plt.title("Feature Importance")
                plt.savefig("feature_importance.png")
                mlflow.log_artifact("feature_importance.png")
                plt.close()
                
                print(f"准确率: {accuracy:.4f}")
                print(f"精确率: {precision:.4f}")
                print(f"召回率: {recall:.4f}")
        
        print("\n实验追踪完成！")
        print(f"查看结果: mlflow ui --backend-store-uri ./mlruns")
        
    except ImportError:
        print("请先安装MLflow: pip install mlflow")

def simple_experiment_tracker():
    """简单的实验追踪器（无外部依赖）"""
    import json
    from datetime import datetime
    
    class ExperimentTracker:
        """轻量级实验追踪器"""
        def __init__(self, experiment_name):
            self.experiment_name = experiment_name
            self.runs = []
            self.current_run = None
        
        def start_run(self, run_name=None):
            """开始新的运行"""
            self.current_run = {
                "run_name": run_name or f"run_{len(self.runs)+1}",
                "start_time": datetime.now().isoformat(),
                "params": {},
                "metrics": {},
                "artifacts": []
            }
            print(f"开始运行: {self.current_run['run_name']}")
        
        def log_param(self, key, value):
            """记录参数"""
            if self.current_run:
                self.current_run["params"][key] = value
        
        def log_params(self, params):
            """记录多个参数"""
            if self.current_run:
                self.current_run["params"].update(params)
        
        def log_metric(self, key, value, step=None):
            """记录指标"""
            if self.current_run:
                if key not in self.current_run["metrics"]:
                    self.current_run["metrics"][key] = []
                self.current_run["metrics"][key].append({
                    "value": value,
                    "step": step,
                    "timestamp": datetime.now().isoformat()
                })
        
        def end_run(self):
            """结束当前运行"""
            if self.current_run:
                self.current_run["end_time"] = datetime.now().isoformat()
                self.runs.append(self.current_run)
                print(f"结束运行: {self.current_run['run_name']}")
                self.current_run = None
        
        def save(self, filepath):
            """保存实验记录"""
            data = {
                "experiment_name": self.experiment_name,
                "runs": self.runs
            }
            with open(filepath, 'w', encoding='utf-8') as f:
                json.dump(data, f, ensure_ascii=False, indent=2)
            print(f"实验记录已保存至: {filepath}")
        
        def get_best_run(self, metric, mode="max"):
            """获取最佳运行"""
            if not self.runs:
                return None
            
            best_run = None
            best_value = float('-inf') if mode == "max" else float('inf')
            
            for run in self.runs:
                if metric in run["metrics"] and run["metrics"][metric]:
                    value = run["metrics"][metric][-1]["value"]
                    if mode == "max" and value > best_value:
                        best_value = value
                        best_run = run
                    elif mode == "min" and value < best_value:
                        best_value = value
                        best_run = run
            
            return best_run
    
    # 使用示例
    tracker = ExperimentTracker("my_experiment")
    
    # 运行1
    tracker.start_run("baseline")
    tracker.log_params({"lr": 0.001, "epochs": 10, "batch_size": 32})
    tracker.log_metric("accuracy", 0.85, step=1)
    tracker.log_metric("accuracy", 0.87, step=2)
    tracker.log_metric("loss", 0.45, step=1)
    tracker.end_run()
    
    # 运行2
    tracker.start_run("improved")
    tracker.log_params({"lr": 0.0001, "epochs": 20, "batch_size": 64})
    tracker.log_metric("accuracy", 0.88, step=1)
    tracker.log_metric("accuracy", 0.91, step=2)
    tracker.log_metric("loss", 0.35, step=1)
    tracker.end_run()
    
    # 保存和查看最佳运行
    tracker.save("experiment_log.json")
    best = tracker.get_best_run("accuracy", mode="max")
    if best:
        print(f"\n最佳运行: {best['run_name']}")
        print(f"最佳准确率: {best['metrics']['accuracy'][-1]['value']}")

if __name__ == "__main__":
    simple_experiment_tracker()
```

## 7 常见问题

**实验管理常见问题**：

1. **实验记录混乱**
   - 原因：命名不规范，缺少标准
   - 解决：制定实验命名规范，建立记录标准

2. **数据量过大**
   - 原因：产物文件过多，占用存储
   - 解决：定期清理，选择性保存关键产物

3. **多人协作困难**
   - 原因：缺少中央服务器
   - 解决：部署MLflow等跟踪服务器

4. **实验难以复现**
   - 原因：环境配置未记录
   - 解决：记录Python版本、依赖包版本

5. **查询效率低**
   - 原因：实验数量庞大
   - 解决：使用标签和分类组织实验

## 8 应用场景

**实验管理的典型应用**：

1. **超参数调优**
   - 系统化搜索最优超参数
   - 比较不同配置的效果

2. **模型选择**
   - 对比多个模型的性能
   - 选择最佳模型

3. **消融实验**
   - 验证每个组件的贡献
   - 理解模型行为

4. **论文实验**
   - 记录实验结果用于报告
   - 可复现的研究

5. **团队知识共享**
   - 共享最佳实践
   - 避免重复工作

---

> 更新时间：2026年4月
