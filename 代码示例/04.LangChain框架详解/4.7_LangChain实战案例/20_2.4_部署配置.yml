# docker-compose.yml
version: '3.8'

services:
  multi-agent:
    build: .
    ports:
      - "8001:8001"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-gpt-5.4-mini}
    restart: unless-stopped
