# 4.1 LangChain基础与架构

## 1 技术简介

**LangChain**是一个用于开发由语言模型驱动的应用程序的框架。它提供了一套工具、组件和接口，使得开发者能够轻松地将LLM（大语言模型）与外部数据源、API、数据库以及其他计算资源连接起来，构建复杂的AI应用。

**核心定位**：
- **LLM应用开发框架**：封装底层复杂性，提供高阶抽象
- **编排引擎**：协调多个组件协同工作（Chains、Agents、Tools）
- **生态连接器**：统一接口对接数百种LLM、向量库、工具
- **生产级工具链**：从原型到部署的完整支持

**LangChain 1.0+版本特性**：
- **LCEL（LangChain Expression Language）**：声明式链式调用语法
- **Runnable接口**：统一的执行接口，支持流式、异步、批处理
- **模块化架构**：清晰的组件边界，易于扩展和定制
- **类型安全**：全面的类型提示支持

**与原生LLM调用的对比**：

| 特性 | 原生API调用 | LangChain |
|------|------------|-----------|
| Prompt管理 | 手动字符串拼接 | 模板化管理，变量注入 |
| 上下文维护 | 手动维护message列表 | Memory组件自动管理 |
| 外部工具 | 需自行实现调用逻辑 | Tools系统标准化集成 |
| 向量检索 | 需自行实现RAG流程 | Retriever抽象，开箱即用 |
| 流式输出 | 需处理Stream解析 | 统一的stream()接口 |
| 生产监控 | 需自行实现 | LangSmith集成，开箱即观测 |

## 2 技术背景

### 2.1 为什么需要LangChain

**原生LLM开发的痛点**：

1. **重复造轮子**：每个项目都需要重新实现Prompt模板、上下文管理、工具调用等基础功能
2. **代码耦合严重**：业务逻辑与LLM调用细节混杂，难以维护和测试
3. **缺乏标准化**：不同LLM提供商的API差异大，切换成本高
4. **复杂流程难以编排**：多步骤、条件分支、循环等复杂逻辑难以优雅实现
5. **生产化困难**：缺乏日志追踪、性能监控、错误处理等生产级功能

**LangChain的解决方案**：

- **组件化设计**：将LLM应用拆解为可复用的原子组件
- **标准化接口**：统一的抽象层，屏蔽底层LLM差异
- **链式编排**：通过LCEL实现复杂流程的声明式定义
- **生态集成**：与OpenAI、Anthropic、HuggingFace等无缝对接

### 2.2 LangChain发展历程

**版本演进**：
- **v0.1（2022年底）**：初始版本，Chain-based架构
- **v0.2（2024年中）**：引入LCEL，Runnable范式
- **v1.0（2024年底）**：稳定API，生产就绪，性能大幅提升

**架构演进**：
```
v0.1: Chain-centric（链为中心）
       ↓
v0.2: Runnable-centric（可运行对象为中心）+ LCEL
       ↓
v1.0: Modular + Type-safe + Production-ready
```

## 3 核心概念

### 3.1 Component（组件）

LangChain应用的基本构建块，每个组件都是可独立使用的功能单元：

- **Chat Models**：对话模型（OpenAI、Anthropic、本地模型等）
- **LLMs**：文本生成模型
- **Prompts**：提示模板（PromptTemplate、ChatPromptTemplate）
- **Output Parsers**：输出解析器（JSON、XML、Pydantic对象等）
- **Document Loaders**：文档加载器（PDF、HTML、数据库等）
- **Text Splitters**：文本分割器（Recursive、Token-based等）
- **Embeddings**：嵌入模型
- **Vector Stores**：向量存储（FAISS、Chroma、Pinecone等）
- **Retrievers**：检索器
- **Tools**：外部工具（搜索、计算、API调用等）
- **Memory**：记忆系统

### 3.2 Chain（链）

将多个组件按顺序组合，形成端到端的处理流程。在v1.0中，推荐使用LCEL替代传统的Chain类。

**传统Chain**：
```python
from langchain.chains import LLMChain
chain = LLMChain(llm=llm, prompt=prompt)
```

**LCEL方式（推荐）**：
```python
chain = prompt | llm | output_parser
```

### 3.3 LCEL（LangChain Expression Language）

LangChain表达式语言，使用管道操作符`|`组合组件：

```python
# 声明式链式调用
chain = (
    {"context": retriever, "question": RunnablePassthrough()}
    | prompt
    | llm
    | StrOutputParser()
)
```

**核心特性**：
- **流式支持**：自动处理stream、astream
- **并行执行**：自动优化并行步骤
- **可观测性**：自动追踪每一步的执行
- **类型传递**：自动传递和转换数据类型

### 3.4 Runnable（可运行对象）

所有LangChain组件都实现的统一接口：

```python
class Runnable(ABC):
    def invoke(self, input) -> Output:
        """同步调用"""
        pass
    
    async def ainvoke(self, input) -> Output:
        """异步调用"""
        pass
    
    def stream(self, input) -> Iterator[Output]:
        """流式输出"""
        pass
    
    async def astream(self, input) -> AsyncIterator[Output]:
        """异步流式"""
        pass
    
    def batch(self, inputs) -> List[Output]:
        """批处理"""
        pass
```

### 3.5 Agent（智能体）

能够自主决策、调用工具、完成复杂任务的系统：

- **AgentExecutor**：执行Agent的运行时环境
- **Tools**：Agent可调用的工具集合
- **Agent Type**：ReAct、Plan-and-Execute、OpenAI Functions等

## 4 工作原理

### 4.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    LangChain Architecture                   │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  Model I/O   │  │   Retrieval  │  │    Agents    │      │
│  │  (LLM/Chat)  │  │  (RAG Flow)  │  │ (Tool Use)   │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                 │                 │              │
│         └─────────────────┼─────────────────┘              │
│                           │                                 │
│  ┌────────────────────────┴────────────────────────┐       │
│  │              LCEL Pipeline Engine                │       │
│  │         (Runnable + Pipe Operator)               │       │
│  └─────────────────────────────────────────────────┘       │
│                           │                                 │
│         ┌─────────────────┼─────────────────┐              │
│         ↓                 ↓                 ↓              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Memory     │  │   Callbacks  │  │  LangSmith   │      │
│  │   (State)    │  │  (Observers) │  │ (Observability)│     │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 执行流程

以RAG应用为例：

```
User Query
    ↓
[Retrieval Step]
    ├── Query Embedding
    ├── Vector Search (Top-K)
    └── Context Assembly
    ↓
[Prompt Construction]
    ├── System Prompt
    ├── Retrieved Context
    └── User Question
    ↓
[LLM Generation]
    ├── Token Generation
    ├── Streaming Output (optional)
    └── Response Completion
    ↓
[Post-processing]
    ├── Output Parsing
    └── Memory Update
    ↓
Final Response
```

### 4.3 LCEL执行机制

```python
# 定义链
chain = component_a | component_b | component_c

# 执行时内部流程：
# 1. component_a.invoke(input) → output_a
# 2. component_b.invoke(output_a) → output_b  
# 3. component_c.invoke(output_b) → final_output

# 流式执行：
# 每个组件的stream()方法被调用，输出通过生成器传递
```

### 4.4 回调系统（Callbacks）

LangChain的观察者模式，用于追踪、监控、日志：

```python
from langchain.callbacks import StdOutCallbackHandler

chain.invoke(
    input,
    config={"callbacks": [StdOutCallbackHandler()]}
)
```

**回调事件**：
- `on_chain_start/end`
- `on_llm_start/end`
- `on_tool_start/end`
- `on_retriever_start/end`

## 5 技术实现

### 5.1 安装与配置

```bash
# 基础安装
pip install langchain

# 包含常用集成
pip install langchain-community

# LangSmith追踪（可选但推荐）
pip install langsmith

# 特定模型支持
pip install langchain-openai
pip install langchain-anthropic
pip install langchain-huggingface
```

### 5.2 环境配置

```python
import os
from langsmith import Client

# OpenAI API Key
os.environ["OPENAI_API_KEY"] = "your-api-key"

# LangSmith配置（用于追踪）
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "your-langsmith-key"
os.environ["LANGCHAIN_PROJECT"] = "my-project"
```

### 5.3 模型选择与配置

```python
from langchain_openai import ChatOpenAI, OpenAIEmbeddings
from langchain_anthropic import ChatAnthropic

# GPT-4配置
chat_model = ChatOpenAI(
    model="gpt-4",
    temperature=0.7,
    max_tokens=2048,
    streaming=True  # 启用流式
)

# Claude配置
claude_model = ChatAnthropic(
    model="claude-3-opus-20240229",
    temperature=0.5
)

# Embedding模型
embeddings = OpenAIEmbeddings(
    model="text-embedding-3-small"
)
```

### 5.4 核心设计模式

**模式1：Basic Chain（基础链）**
```
Prompt Template → LLM → Output Parser
```

**模式2：RAG Chain（检索增强）**
```
Query → Retriever → Context + Query → Prompt → LLM → Output
```

**模式3：Agent with Tools（工具代理）**
```
Query → Agent → [Tool Call → Tool Result]* → Final Answer
```

**模式4：Multi-Chain（多链组合）**
```
Input → Branch A → LLM → Output A
      → Branch B → LLM → Output B
      → Combine → Final Output
```

## 6 Python代码实现与逐行解析

### 6.1 基础链式调用

```python
"""
LangChain基础链式调用示例
演示最基本的Prompt + LLM + Output Parser组合
"""

from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser

# ============================================================
# 步骤1：创建Chat模型实例
# ============================================================
# ChatOpenAI是LangChain对OpenAI Chat Completion API的封装
# model: 使用的模型名称，gpt-3.5-turbo性价比高，gpt-4能力强
# temperature: 控制输出随机性，0.0最确定，1.0最随机
# streaming: 是否启用流式输出
llm = ChatOpenAI(
    model="gpt-3.5-turbo",
    temperature=0.7,
    streaming=True
)

# ============================================================
# 步骤2：定义Prompt模板
# ============================================================
# ChatPromptTemplate.from_messages() 用于定义多轮对话模板
# SystemMessage: 设定AI助手的行为和角色
# HumanMessage: 用户的输入，{topic}是模板变量
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个专业的技术文档撰写专家，擅长用通俗易懂的语言解释复杂概念。"),
    ("human", "请用3个要点解释什么是{topic}，每个要点不超过50字。")
])

# ============================================================
# 步骤3：创建输出解析器
# ============================================================
# StrOutputParser将模型的AIMessage输出转换为纯字符串
# 简化后续处理，不需要手动提取.content
output_parser = StrOutputParser()

# ============================================================
# 步骤4：使用LCEL组合链式调用
# ============================================================
# 管道操作符 | 用于组合组件，数据从左到右流动
# 执行顺序：输入 → prompt模板渲染 → LLM调用 → 输出解析
chain = prompt | llm | output_parser

# ============================================================
# 步骤5：执行链式调用（同步）
# ============================================================
# invoke()方法接收字典作为输入，对应模板中的变量
# {"topic": "机器学习"} 会填充模板中的{topic}占位符
result = chain.invoke({"topic": "机器学习"})
print("同步调用结果：")
print(result)

# ============================================================
# 步骤6：流式输出（实时显示）
# ============================================================
# stream()方法返回生成器，可以逐token获取输出
# 适合UI实时显示，提升用户体验
print("\n流式输出：")
for chunk in chain.stream({"topic": "深度学习"}):
    # chunk是字符串片段，直接打印不换行实现打字机效果
    print(chunk, end="", flush=True)
print()

# ============================================================
# 步骤7：批处理（批量调用）
# ============================================================
# batch()方法接收输入列表，并行处理多个请求
# 内部自动管理并发，提高效率
inputs = [
    {"topic": "区块链"},
    {"topic": "云计算"},
    {"topic": "物联网"}
]
results = chain.batch(inputs)
print("\n批处理结果：")
for i, result in enumerate(results, 1):
    print(f"{i}. {result[:100]}...")
```

### 6.2 RAG完整实现

```python
"""
基于LangChain的完整RAG系统实现
包含文档加载、分割、向量化、检索、生成全流程
"""

from langchain_community.document_loaders import TextLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings, ChatOpenAI
from langchain_community.vectorstores import FAISS
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
from langchain_core.runnables import RunnablePassthrough

# ============================================================
# 步骤1：加载文档
# ============================================================
# TextLoader用于加载纯文本文件
# 支持多种编码，自动处理文件IO
loader = TextLoader("./data/document.txt", encoding="utf-8")
documents = loader.load()  # 返回Document对象列表
# Document结构：page_content(文本内容), metadata(元数据字典)

# ============================================================
# 步骤2：文档分割
# ============================================================
# RecursiveCharacterTextSplitter是推荐的分割器
# chunk_size: 每个块的目标字符数
# chunk_overlap: 相邻块的重叠字符数（保持上下文连贯）
# separators: 分割时优先尝试的分隔符列表
splitter = RecursiveCharacterTextSplitter(
    chunk_size=500,
    chunk_overlap=50,
    separators=["\n\n", "\n", "。", "，", " ", ""]
)
chunks = splitter.split_documents(documents)
print(f"文档分割为 {len(chunks)} 个片段")

# ============================================================
# 步骤3：创建向量存储
# ============================================================
# OpenAIEmbeddings使用text-embedding-ada-002模型（默认）
# 将文本转换为1536维向量
embeddings = OpenAIEmbeddings()

# FAISS是Facebook开源的高效向量相似度搜索库
# from_documents()自动完成：嵌入生成 + 索引构建
vector_store = FAISS.from_documents(chunks, embeddings)

# 保存索引到磁盘（可选）
vector_store.save_local("./faiss_index")

# ============================================================
# 步骤4：创建检索器
# ============================================================
# as_retriever()将VectorStore转换为Retriever接口
# search_kwargs控制检索行为：
#   - k: 返回最相似的k个文档
#   - score_threshold: 相似度阈值过滤
retriever = vector_store.as_retriever(
    search_type="similarity",  # 相似度搜索（默认）
    search_kwargs={"k": 4}     # 返回Top-4文档
)

# ============================================================
# 步骤5：定义RAG Prompt模板
# ============================================================
# {context}将由检索器自动填充
# {question}是用户的原始问题
rag_prompt = ChatPromptTemplate.from_template("""
基于以下上下文信息回答问题。如果上下文中没有相关信息，请说明无法回答。

上下文：
{context}

问题：{question}

请提供详细且准确的答案：
""")

# ============================================================
# 步骤6：创建格式化函数
# ============================================================
def format_docs(docs):
    """
    将检索到的文档列表格式化为字符串
    每个文档包含page_content和metadata
    """
    return "\n\n".join([
        f"[文档 {i+1}] {doc.page_content}"
        for i, doc in enumerate(docs)
    ])

# ============================================================
# 步骤7：使用LCEL构建RAG链
# ============================================================
# RunnablePassthrough()将输入原样传递，用于保持question字段
# retriever.invoke(question)检索相关文档
# format_docs将文档列表转为字符串作为context
# 最终输入到prompt模板：{context, question}
rag_chain = (
    {
        "context": retriever | format_docs,  # 检索并格式化文档
        "question": RunnablePassthrough()     # 保留原始问题
    }
    | rag_prompt    # 填充模板
    | llm           # 调用LLM
    | StrOutputParser()  # 解析输出
)

# ============================================================
# 步骤8：执行RAG查询
# ============================================================
question = "LangChain的主要特点是什么？"

# 同步调用
answer = rag_chain.invoke(question)
print(f"问题：{question}")
print(f"答案：{answer}")

# 查看检索到的文档（用于调试）
retrieved_docs = retriever.invoke(question)
print(f"\n检索到 {len(retrieved_docs)} 个相关片段")
for i, doc in enumerate(retrieved_docs, 1):
    print(f"{i}. {doc.page_content[:100]}...")
```

### 6.3 Agent与Tools实现

```python
"""
LangChain Agent实现示例
演示如何创建能自主调用工具的AI智能体
"""

from langchain_openai import ChatOpenAI
from langchain.agents import AgentExecutor, create_openai_functions_agent
from langchain_core.tools import Tool
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
import requests
import math

# ============================================================
# 步骤1：定义工具函数
# ============================================================
def search_web(query: str) -> str:
    """
    模拟网络搜索工具
    实际项目中可接入Google Search API、Bing API等
    """
    # 这里使用模拟数据，实际应调用搜索API
    return f"搜索结果：关于'{query}'找到相关信息..."

def calculate(expression: str) -> str:
    """
    计算器工具
    使用eval计算数学表达式（生产环境需安全检查）
    """
    try:
        # 白名单允许的字符，防止代码注入
        allowed_chars = set('0123456789+-*/(). ')
        if not all(c in allowed_chars for c in expression):
            return "错误：表达式包含非法字符"
        
        result = eval(expression)
        return f"计算结果：{result}"
    except Exception as e:
        return f"计算错误：{str(e)}"

def get_weather(city: str) -> str:
    """
    模拟天气查询工具
    实际应接入天气API（如OpenWeatherMap）
    """
    weather_data = {
        "北京": "晴天，25°C",
        "上海": "多云，28°C",
        "广州": "小雨，30°C"
    }
    return weather_data.get(city, f"未找到{city}的天气信息")

# ============================================================
# 步骤2：创建Tool对象
# ============================================================
# Tool是LangChain中工具的封装
# name: 工具名称，Agent通过名称识别和调用
# func: 实际执行的函数
# description: 工具功能描述，帮助Agent决定何时使用
tools = [
    Tool(
        name="web_search",
        func=search_web,
        description="当需要查询最新信息、新闻或不确定的知识时使用"
    ),
    Tool(
        name="calculator",
        func=calculate,
        description="当需要进行数学计算时使用，支持加减乘除和括号"
    ),
    Tool(
        name="weather",
        func=get_weather,
        description="当需要查询某个城市的天气时使用，输入城市名"
    )
]

# ============================================================
# 步骤3：创建LLM实例
# ============================================================
# Agent需要支持function calling的模型
# OpenAI的gpt-3.5-turbo和gpt-4都支持
llm = ChatOpenAI(
    model="gpt-3.5-turbo",
    temperature=0  # Agent通常使用确定性输出
)

# ============================================================
# 步骤4：定义Agent Prompt
# ============================================================
# MessagesPlaceholder用于Agent内部维护对话历史
# agent_scratchpad存储中间思考过程
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个有用的AI助手，可以使用工具帮助用户解决问题。"),
    ("human", "{input}"),
    MessagesPlaceholder(variable_name="agent_scratchpad")
])

# ============================================================
# 步骤5：创建Agent
# ============================================================
# create_openai_functions_agent使用OpenAI的Function Calling机制
# 这是目前最可靠的Agent实现方式
agent = create_openai_functions_agent(llm, tools, prompt)

# ============================================================
# 步骤6：创建Agent执行器
# ============================================================
# AgentExecutor负责运行Agent的循环：
# 1. 接收用户输入
# 2. 调用LLM决定行动
# 3. 如有工具调用，执行工具并返回结果
# 4. 重复直到得到最终答案
agent_executor = AgentExecutor(
    agent=agent,
    tools=tools,
    verbose=True,  # 打印详细执行过程
    max_iterations=10  # 最大循环次数，防止无限循环
)

# ============================================================
# 步骤7：执行Agent
# ============================================================
# Agent会自动分析用户意图，决定是否需要调用工具
# 并自主完成多步骤任务
response = agent_executor.invoke({
    "input": "计算 1234 * 5678 是多少，并告诉我上海的天气"
})

print(f"最终回答：{response['output']}")
```

### 6.4 Memory记忆系统

```python
"""
LangChain Memory系统实现
演示如何维护对话上下文和长期记忆
"""

from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
from langchain_community.chat_message_histories import ChatMessageHistory
from langchain_core.runnables.history import RunnableWithMessageHistory

# ============================================================
# 步骤1：创建消息历史存储
# ============================================================
# ChatMessageHistory内存存储对话历史
# 实际项目可使用Redis、数据库等持久化存储
session_histories = {}

def get_session_history(session_id: str):
    """
    获取或创建会话历史
    用于管理多用户/多会话的独立对话上下文
    """
    if session_id not in session_histories:
        session_histories[session_id] = ChatMessageHistory()
    return session_histories[session_id]

# ============================================================
# 步骤2：定义带历史的Prompt
# ============================================================
# MessagesPlaceholder(variable_name="history")会被自动填充历史消息
# 保持对话的连续性，让模型理解上下文
prompt_with_history = ChatPromptTemplate.from_messages([
    ("system", "你是一个友好的对话助手，记住之前的对话内容。"),
    MessagesPlaceholder(variable_name="history"),  # 自动插入历史消息
    ("human", "{input}")
])

# ============================================================
# 步骤3：创建基础链
# ============================================================
llm = ChatOpenAI(model="gpt-3.5-turbo")
chain = prompt_with_history | llm

# ============================================================
# 步骤4：包装为带历史的链
# ============================================================
# RunnableWithMessageHistory自动管理：
# - 从存储加载历史
# - 添加到Prompt
# - 保存新消息到存储
chain_with_history = RunnableWithMessageHistory(
    chain,
    get_session_history,
    input_messages_key="input",
    history_messages_key="history"
)

# ============================================================
# 步骤5：多轮对话示例
# ============================================================
session_id = "user_123"

# 第一轮
response1 = chain_with_history.invoke(
    {"input": "我叫张三，是一名软件工程师。"},
    config={"configurable": {"session_id": session_id}}
)
print(f"AI: {response1.content}")

# 第二轮（AI应该记得用户名字）
response2 = chain_with_history.invoke(
    {"input": "你还记得我的名字吗？"},
    config={"configurable": {"session_id": session_id}}
)
print(f"AI: {response2.content}")

# 查看完整对话历史
history = get_session_history(session_id)
print("\n完整对话历史：")
for msg in history.messages:
    print(f"{msg.type}: {msg.content}")
```

### 6.5 流式输出与追踪

```python
"""
LangChain流式输出和LangSmith追踪
生产环境常用的高级功能
"""

from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate
from langchain.callbacks import StreamingStdOutCallbackHandler
from langchain.callbacks.tracers import LangChainTracer

# ============================================================
# 步骤1：配置回调处理器
# ============================================================
# StreamingStdOutCallbackHandler: 实时打印输出到控制台
# LangChainTracer: 发送追踪数据到LangSmith平台
callbacks = [
    StreamingStdOutCallbackHandler(),
    LangChainTracer(project_name="demo-project")
]

# ============================================================
# 步骤2：创建带回调的LLM
# ============================================================
llm = ChatOpenAI(
    model="gpt-3.5-turbo",
    streaming=True,  # 必须启用streaming
    callbacks=callbacks
)

# ============================================================
# 步骤3：定义链
# ============================================================
prompt = ChatPromptTemplate.from_template("写一篇关于{topic}的短文，100字左右。")
chain = prompt | llm

# ============================================================
# 步骤4：流式调用
# ============================================================
# 流式输出会触发callbacks的on_llm_new_token事件
print("正在生成内容...")
result = chain.invoke({"topic": "人工智能的未来"})

# ============================================================
# 步骤5：在LangSmith查看追踪
# ============================================================
# 访问 https://smith.langchain.com
# 可以看到：
# - 完整的调用链
# - 每个步骤的输入输出
# - Token使用量和延迟
# - 错误和异常信息
print(f"\n\n完整输出：{result.content}")
```

## 7 常见问题

### Q1: LangChain v0.x和v1.0有什么区别？

**主要差异**：
- **v0.x**: 基于`Chain`类，继承复杂，调试困难
- **v1.0**: 基于`Runnable`接口和LCEL，更灵活、类型安全

**迁移建议**：
- 新项目直接使用v1.0
- 旧项目参考官方迁移指南逐步更新

### Q2: 如何选择合适的模型？

**选择建议**：
| 场景 | 推荐模型 | 说明 |
|------|---------|------|
| 简单问答 | gpt-3.5-turbo | 成本低，速度快 |
| 复杂推理 | gpt-4 | 能力强，准确率高 |
| 长文本处理 | gpt-4-turbo | 128K上下文 |
| 代码生成 | Claude-3-Opus | 代码能力突出 |
| 本地部署 | llama2-70b | 隐私敏感场景 |

### Q3: 如何处理长文档超出上下文限制？

**解决方案**：
1. **Map-Reduce模式**：分段处理，汇总结果
2. **Refine模式**：逐步精炼答案
3. **检索增强**：只加载相关片段（RAG）

### Q4: Agent陷入无限循环怎么办？

**解决方法**：
```python
AgentExecutor(
    agent=agent,
    tools=tools,
    max_iterations=5,  # 限制最大迭代次数
    early_stopping_method="generate"  # 超时停止策略
)
```

### Q5: 如何优化RAG的检索质量？

**优化策略**：
1. **分块策略**：调整chunk_size和overlap
2. **查询重写**：使用LLM优化用户查询
3. **混合检索**：结合关键词和向量检索
4. **重排序**：使用Cross Encoder精排结果

## 8 应用场景

### 8.1 企业知识库问答

**场景**：构建基于公司内部文档的智能问答系统

**技术栈**：
- Document Loaders加载PDF/Word/网页
- Vector Store存储文档向量
- RAG Chain实现检索增强生成
- Memory维护对话上下文

### 8.2 AI数据分析助手

**场景**：通过自然语言查询数据库、生成报表

**技术栈**：
- SQL Database Toolkit
- Agent执行多步数据分析
- Tools封装数据库查询
- Output Parser格式化输出

### 8.3 智能客服机器人

**场景**：24/7在线客服，处理常见咨询

**技术栈**：
- ConversationalRetrievalChain
- Memory维护会话状态
- Fallback到人工客服
- 集成企业工单系统

### 8.4 代码生成与审查

**场景**：辅助编程、代码审查、文档生成

**技术栈**：
- OpenAI Codex或其他代码模型
- File System Tools操作代码文件
- Agent规划复杂重构任务
- Callbacks记录修改历史

### 8.5 多Agent协作系统

**场景**：模拟团队协作，不同Agent扮演不同角色

**技术栈**：
- LangGraph编排多Agent工作流
- 消息总线实现Agent通信
- Shared Memory共享上下文
- Supervisor Agent协调任务分配

---

**下一步学习**：掌握LangChain基础后，建议深入学习5.2-5.7节，了解各核心组件的详细用法和高级特性。
