# AI应用开发技术栈目录 - 修改记录 v4.4

> 本文档记录 2026 年 9 月对学习路线库（14 章 77 节）的一次**增量技术校订**：更新过时知识点、修正错误示例代码。本次为校订而非重写，文档骨架保持不变。

---

## 〇、校订方式与不变量

- **范围**：全部 14 章 77 个小节文件全量复核。
- **只改**：正文事实 / 措辞、代码块内部。
- **不变**：标题文本与编号、章节顺序、小节数量、表格列结构、代码围栏语义、篇幅（±10% 内）、CRLF 行尾；未借校订扩写或新增章节。
- **结果**：51 个文件有实质更新（尾注改为 `> 更新时间：2026年9月`，带后缀的保留后缀）；26 个文件体检无过时点，保持原样（尾注仍为 2026年4月）。
- **代码复验**：全量 1128 个 Python 代码块经独立 AST 语法复验（栈式围栏配对 + 三引号字符串感知 + git 原版 A/B 比对）。
- **不联网原则**：无法核实的版本号 / API 签名一律保留可运行原文，列入第五节存疑项，未编造。

---

## 一、过时知识点更新（按章）

### 第 1 章 Python 快速入门（6/6 更新）
- 泛型语法对齐 **PEP 695**（Python 3.12+）；补充 Python 3.13/3.14 时间线与 **PEP 703/779 free-threaded（无 GIL）**构建。
- 包管理更新为 **uv + pyproject.toml / uv.lock**，lint 工具链更新为 **ruff**（替代 black + flake8）。
- `asyncio.get_event_loop()` → `get_running_loop()`；修正 MRO、Go 多赋值等表述性错误。

### 第 2 章 AI 底层基础（7/9 更新）
- **PyTorch 2.x**：`torch.cuda.amp` / 无参 `autocast()` 统一迁移到 `torch.amp.autocast("cuda", ...)`、`GradScaler` 新命名空间（2.4 / 2.7 / 2.8，跨文件一致）。
- 修正 PEFT 配置名 `P-tuningV2Config` → `PTuningV2Config`；原理性数学公式与经典结论保持不动。

### 第 3 章 LLM 工程基础（3/3 更新）
- **3.2 Function Calling**：解决"开头声明 Responses API 为推荐范式、正文却是 Chat Completions"的范式不一致——全文统一为 `client.responses.create(...)` + 扁平工具定义 + `function_call` / `function_call_output` / `output_text` 回传链路；旧代称 o3/o4-mini 更新为 GPT-5.4 系列。
- **3.3 Structured Output**：开头声明与正文 `response_format` / JSON Schema 代码对齐。
- **3.1 Prompt Engineering**：示例模型更新为当前系列。

### 第 4 章 LangChain 框架（7/7 更新）
- 统一 **v1.x** 基线：`create_agent` + Middleware、`langchain-core>=1.2.31`、Python 3.10+；导入路径迁移（`langchain_openai` / `langchain_huggingface` 等）。
- 旧 `AgentExecutor` / `initialize_agent` / `LLMChain`、旧 Memory 类、旧 Retriever / OutputParser 用法按 v1.x 修正或加迁移注释；v1.x 具体签名不确定处保留可运行原文（见存疑项）。

### 第 5 章 RAG（3/6 更新）
- `SemanticChunker` 错误参数 `threshold` → `breakpoint_threshold_type`；不存在的 `RecursiveCodeTextSplitter` → `RecursiveCharacterTextSplitter.from_language(...)`。
- Embedding 客户端改 `OpenAI(api_key=...)` 实例化，`dimensions=None` 不再发 null；清理失效的 `create_retrieval_chain` 导入并标注旧链迁移。

### 第 6 章 Agent 系统（3/9 更新）
- **6.5 MCP**："当前为 2024-11-05" 硬编码表述改为按 `protocolVersion` 握手协商。
- 6.8 框架对比年份更新到 2026；6.9 Multi-Agent 补充 **A2A 互操作协议**。

### 第 7 / 8 章 向量数据库（5/7 更新）
- **Chroma**：`Client(Settings(persist_directory=...))` + `.persist()` → `PersistentClient(path=...)`（自动持久化），删除无效 `Settings` 导入；后端示例与维度标注纠错。
- **FAISS**：不存在的 `faiss.__version__` → `getattr(faiss, "__version__", ...)`；`get_relevant_documents` → `invoke`。
- **PGVector**：补 `psycopg2.extras` 的 `Json` 缺失导入（真实 NameError）。

### 第 9 / 10 章 数据处理与模型部署（8/9 更新）
- pandas 已移除的 `fillna(method='ffill')` / 链式 `inplace` → `ffill()` / `bfill()` / 直接赋值；`llm.predict` → `invoke`。
- **ONNX**：opset 建议 11/13 → **17/18**（PyTorch 2.x 默认），导出循环与 `output_names` 笔误修正。
- **量化**：GPTQ 全称订正；`torch.quantization.quantize_fx.convert(state_dict)` 误用改为作用于模块；`resnet50(pretrained=True)` → `weights=` API；拼写订正。
- **推理服务**：`python -m vllm.entrypoints.openai.api_server` → **`vllm serve`**；benchmark / docker / 4-bit 示例 Llama-2 → Llama-3.1；embedding 旧名 → `text-embedding-3-small`。

### 第 11 章 AI 工程化（3/5 更新）
- 修正 print 多余 f 前缀、时间戳示例年份、补 `timedelta` 导入并去除 `__import__('datetime')` hack。

### 第 12 / 14 章 实战项目与应用开发（5/11 更新）
- `OpenAIEmbeddings` 等导入迁移到 `langchain_openai`；`RetrievalQA` / 旧链加迁移注释。
- **FastAPI** 弃用的 `@app.on_event("startup")` → `lifespan` + `asynccontextmanager`；`ahttp` → `aiohttp`；`gpt-3.5` → `gpt-5.4-mini`。

### 第 13 章 系统架构设计（1/5 更新）
- **13.4 MCP 工具架构**：传输层表述 HTTP/SSE → **Streamable HTTP**（仅改标签，架构图结构不动）。

---

## 二、示例代码修正（AST 复验通过）

校订中修复了大量历史遗留的代码块缺陷，代表类型：

| 类型 | 代表文件 |
|------|----------|
| 开栏粘连 ```` ```pythonimport ```` | 2.1、2.6、12.7 |
| 闭栏贴在代码行尾 `code```` | 2.1、3.1、12.2、4.3 |
| 代码块缺闭栏（吞掉后续正文/末块未闭合） | 10.1（2 处）、4.3、12.2（2 处） |
| 伪代码 / 讲解被误标为 `python`（AST 必失败）→ 改 `text` 或注释化 | 2.5、2.6、1.6 |
| 三引号字符串 / 围栏胶连、孤儿 import、缩进断裂 | 2.6、2.7、2.8、2.9、1.5、12.1 |
| 非法或失效语法（tqdm 条件 import、多余括号、`self layers`、变量未闭合等） | 1.2、1.3、2.1、12.1、12.2、11.4 |

全量 **1128 个 Python 代码块 AST 复验 0 真实语法错误**；shell / Dockerfile / JSON / JS 等非 Python 块人工核对。

---

## 三、Meta 独立复验补修（各章提取器漏检项）

在各域自验之外，Meta 用更严格的解析器做全仓独立复验，额外修复 5 处：

1. **9.4_LLM数据工程.md**：三级标题编号笔误 `### 8.4.1 / 8.4.2 / 8.4.3`（从第 8 章粘贴的三段式）→ 按全书惯例（二级序号.小序号）改为 `### 6.1 / 6.2 / 6.3`（标题文字、层级、小节数不变）。
2. **2.1_Python进阶.md**：L260 开栏粘连拆行；L2021 贴行闭栏独立成行（修复后该性能分析块 AST 才真正通过）。
3. **3.1_Prompt_Engineering全体系.md**：L2607 贴行闭栏独立成行（此前错位吞掉后续 python 块）。
4. **10.1_模型导出与格式转换.md**：补 2 个缺失闭栏（17.1 块尾、文件末尾），使两段长示例正确分段。
5. **4.3_Memory组件详解.md**：末块 `AsyncPersistentMemory` 补顶层 `import json`（原误置于方法体中部导致缩进断裂）、闭栏独立成行并补缺失闭栏。

---

## 四、保持不动的历史结构（非缺陷）

- **复合教学块**：4.5 / 4.6 / 4.7 中存在"Python 示例用 `page_content="""..."""` 三引号字符串内嵌整份 Markdown 文档"的写法，字符串内含 ```` ```bash ```` / ```` ```json ```` 子片段及 `{language}` 模板占位符。这是**合法 Python**（v4.3 原有），朴素围栏提取器会误报"未闭合字符串"，git A/B 比对确认本次零改动，按"围栏结构不动"原则保留。
- **26 个体检干净的文件**：无过时知识点与代码错误，尾注保留 2026年4月。
- 经典原理、数学公式、架构图结构、章节骨架一律不动。

---

## 五、存疑项（未联网核实，保留原文，未编造）

- 3.2 "2026 年 2 月 gpt-4o 退役"时间点、Responses API 内置工具名（web_search / file_search）转正时间。
- 6.5 / 12.5 MCP 客户端默认 `protocol_version="2024-11-05"`；6.5 "MCP 2.0（2025 春夏）"段。
- 8.2 pymilvus ORM 写法（仍可运行，未改写为 MilvusClient 新 API）；8.3 qdrant `recreate_collection` 弃用标注、weaviate.Client v3 签名。
- 10.4 TGI `--quantize bitsandbytes-nf4` 是否弃用、TensorRT-LLM runtime API（跨版本变动大）。
- 10.2 `torch.quantization`（`torch.ao.quantization` 弃用别名，当前仍可用）；10.1 部分 `opset_version=13`（合法稳定版）。
- 14.3 `gr.Interface` 旧 API（迁移 Blocks 需重构，超出骨架约束）。
- 第 1 章 PEP 703/779 free-threaded 的成熟度结论基于知识截止，2026-09 后续变化未联网核实。
- 各框架 / 库最新版本号均未联网查证，不确定处保留原文。

---

## 六、文件改动统计

| 章 | 文件数 | 实际更新 | 章 | 文件数 | 实际更新 |
|----|:--:|:--:|----|:--:|:--:|
| 01 Python 入门 | 6 | 6 | 09 数据处理 | 4 | 3 |
| 02 AI 底层基础 | 9 | 7 | 10 部署推理 | 5 | 5 |
| 03 LLM 工程 | 3 | 3 | 11 工程化工具链 | 5 | 3 |
| 04 LangChain | 7 | 7 | 12 实战项目 | 7 | 4 |
| 05 RAG | 6 | 3 | 13 架构设计 | 5 | 1 |
| 06 Agent 系统 | 9 | 3 | 14 应用开发 | 4 | 1 |
| 07 向量数据库 | 2 | 2 | **合计** | **77** | **51** |
| 08 向量库进阶 | 5 | 3 | 未改动 | — | 26 |

---

*记录人：BlockMemoryAgent（9 个领域 Agent 并行校订 + Meta 独立 AST 复验）*  
*记录时间：2026 年 9 月 12 日*  
*版本：v4.4*
